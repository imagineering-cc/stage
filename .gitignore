node_modules/
*.log
.DS_Store
stage-state.json
stage-state.json.*.tmp

# Local-only venue secrets (WiFi creds, Pi Tailscale IP, exact LAN addresses)
*.local.md
ops/network.local.md

# Disk-alert secrets drop-in (Telegram bot token + chat id). NEVER commit.
# Only the *.example template is tracked.
*.local.env
ops/alert.local.env

# agent isolation worktrees (never commit these)
.claude/worktrees/
